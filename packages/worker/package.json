{
  "name": "@chronopost/worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "wrangler build",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@chronopost/database": "workspace:*",
    "@chronopost/shared": "workspace:*",
    "@atproto/api": "^0.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.0.0"
  }
}
